services:
  - type: web
    name: fastapi-image-endpoint
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server_main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
